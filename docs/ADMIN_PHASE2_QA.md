# Phase 2 QA checklist

- [ ] Edit Restaurant POS text → refresh `/restaurant-pos` → see change
- [ ] Edit Website text / demo copy → refresh `/website` → see change
- [ ] Edit Software product / process copy → refresh `/software` → see change
- [ ] Upload image → paste URL into a solutions hero → image shows (blank URL keeps built-in)
- [ ] CTAs still go to `/contact`
- [ ] No broken images on desktop
- [ ] Mobile layout still looks good on all three pages
- [ ] `/owadmin` still hidden from public nav

## Phase 2 done when

All main marketing pages are editable: Home, About, Pricing, Contact, Restaurant POS, Website, Software.

## Quick verify

1. Login `/owadmin`
2. Open **Restaurant POS** → change hero headline → Save → open `/restaurant-pos`
3. Open **Website** → change demo brand → Save → open `/website`
4. Open **Software** → change process step title → Save → open `/software`
